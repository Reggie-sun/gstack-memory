# Test Plan
Generated by /plan-eng-review on 2026-04-25
Branch: feature/retrieval-priority
Repo: reggie/Enterprise-grade_RAG

## Affected Pages/Routes
- `make agent-inspect` on control-plane changes — verify internal runtime schema changes stay out of stable API contract handling.
- `make agent-preflight TASK=...` on control-plane tasks — verify current task planning artifacts do not create false out-of-scope failures.
- `make agent-preflight TASK=...` on mixed `runtime_host + api_schema` changes — verify the strictest tier / enforcement path wins.
- Cross-window plan recovery flow (`docs/superpowers/plans/*` + `.workflow/active/*`) — verify one narrative source still preserves machine-readable continuity.

## Key Interactions to Verify
- Edit `backend/app/schemas/agent_runtime.py` and confirm inspect reports `runtime_host`, not `api_schema`.
- Edit `backend/app/schemas/chat.py` and confirm inspect still reports `api_schema` with main-contract reads.
- Run preflight on a control-plane task that includes the current plan, current session artifact, and `TODOS.md`, then verify only bound artifacts are ignored.
- Run preflight on a mixed `runtime_host + api_schema` change and verify the strictest enforcement/tier view wins.

## Edge Cases
- Unrelated docs or stale session artifacts should still fail scope checks.
- Unknown high-risk paths should still require a task contract even when planning artifacts are present.
- If docs plan becomes the only narrative source, `.workflow/active` must still keep enough machine summary/link data for handoff.

## Critical Paths
- Internal runtime packet change -> inspect -> runtime_host classification -> focused review path.
- Control-plane cleanup -> preflight with current task artifacts -> pass without broad scope inflation.
- Mixed runtime_host + api_schema change -> inspect/preflight -> strictest tier / hard-block behavior.
- Docs plan as narrative truth -> session summary link -> next window can resume without duplicate full plan text.
