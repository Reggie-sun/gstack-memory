# Test Plan
Generated by /plan-eng-review on 2026-05-12
Branch: main
Repo: Enterprise-grade_RAG

## Affected Pages/Routes
- No frontend pages or routes. This is an eval-governance CLI/doc change.

## Key Interactions to Verify
- Run `python scripts/eval_optimization_cases.py --case-file eval/optimization_cases.yaml --case-id <id> --dry-run` and confirm it prints resolved samples, missing evidence, and command/report preview.
- Confirm dry-run does not mutate `eval/retrieval_samples.yaml`, rolling truth registries, or frozen promotion history.

## Edge Cases
- Missing `sample_refs.path` returns a clear error.
- Missing sample id returns a clear error.
- Duplicate sample id returns a clear error.
- Unsupported `sample_refs.source` returns a clear error.
- Missing evidence path is reported as missing evidence, not treated as a runner crash.

## Critical Paths
- Draft optimization case references one `retrieval_samples` sample and one `trace_candidates` sample, then dry-run resolves both without writing any truth-source files.
