# Test Plan
Generated by /plan-eng-review on 2026-04-15
Branch: feature/retrieval-priority
Repo: Enterprise-grade_RAG

## Affected Pages/Routes
- N/A, docs-only control-plane change

## Key Interactions to Verify
- 新窗口按 `AGENTS.md -> session-handoff -> INGESTION_QUALITY_PLAN.md -> INGESTION_QUALITY_BACKLOG.md -> 当前 lane plan` 顺序启动
- `/ingestion-quality` 输出包含完整 handoff 字段约束，不再漏掉 `Allowed Paths`
- `rebuild-canary` 被理解为 standing regression surface，而不是必须单独起 lane plan 的 active lane

## Edge Cases
- retrieval / rerank / router 任务不会被误导到 ingestion-quality 主线
- standing surface 不会被误写成 active lane 的独立 plan 要求
- session-handoff 不会复制 backlog，只保留轻量接力字段

## Critical Paths
- 控制面优先级在 ingestion-quality 范围内生效，但不会被误读成全局覆盖 `AGENTS.md`
- backlog、plan、command 三份文档对 lane / standing surface / handoff schema 的定义保持一致
