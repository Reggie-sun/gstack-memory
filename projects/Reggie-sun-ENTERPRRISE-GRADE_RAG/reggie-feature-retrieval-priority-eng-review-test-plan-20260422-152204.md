# Test Plan
Generated by /plan-eng-review on 2026-04-22
Branch: feature/retrieval-priority
Repo: Enterprise-grade_RAG

## Affected Pages/Routes
- runtime host SDK helper path — verify passive trigger only admits repo code workflow prompts
- `scripts/codex_runtime_nl_entry.mjs` CLI — verify `--check-entry` and prompt-mode runtime routing stay aligned
- `scripts/agent_runtime_host_adapter_example.mjs` example adapter — verify eligibility decision and prompt-mode runtime handoff

## Key Interactions to Verify
- Prompt with explicit specialized entry, should bypass passive runtime
- Prompt with direct code task intent, should enter runtime and preserve trace
- Prompt with non-repo/general-chat intent, should bypass passive runtime once intent gating is added
- `--paths` plus later flags should parse consistently in both CLI wrappers

## Edge Cases
- Empty prompt
- Explicit skill/lane/command plus changed paths at the same time
- Prompt-only input with active file/open tabs but no changed paths
- General prompt that is not a repo task but also does not use an explicit prefix

## Critical Paths
- `evaluateRuntimeWorkflowEntry(...)` decides bypass vs runtime admission
- `runWorkflowFromPromptWithTrace(...)` preserves visible trace and runtime result
- Example adapter uses the same entry logic as the Codex CLI, not a divergent fork
