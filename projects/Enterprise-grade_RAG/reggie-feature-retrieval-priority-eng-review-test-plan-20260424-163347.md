# Test Plan
Generated by /plan-eng-review on 2026-04-24 16:33:47 CST
Branch: feature-retrieval-priority
Repo: Enterprise-grade_RAG

## Affected Pages/Routes
- `workflow.resolve` runtime path, verify that stage-1 packet fields are emitted only at explicit plan/verify gates, because this is where auto-trigger can silently over-fire.
- `workflow.advance` local strategy path, verify that pending-command status, packet propagation, and post-execution receipts stay consistent after host-side execution.
- `scripts/codex_strategy_runner.mjs` dry-run and exec contracts, verify that allowlisted helper requests are surfaced structurally and that prompt text does not become the only policy layer.

## Key Interactions to Verify
- Resolve a runtime_host/control_plane task contract, confirm the result includes packet-ready stage-1 data without adding a second routing source.
- Run local `agent.execute_strategy`, confirm the host preserves packet payloads and stale completed/skipped pending commands still block auto-advance.
- Trigger `harness.verify`-eligible flows, confirm stage-1 verifier gating uses explicit runtime fields rather than invented reviewer artifacts.

## Edge Cases
- Missing structured repo context should block packet creation instead of silently guessing.
- `recommended_checks` must never be promoted to `required_checks`.
- `manual_only_skills` must override any auto-trigger roster item.
- Stale `pending_command.status=completed|skipped` must block resume even if `transition_gate=auto_advance_allowed`.
- Packet fields added in backend but not normalized in SDK/runner must fail tests, not degrade silently.

## Critical Paths
- `resolve_workflow_capability()` -> host `normalizeExecutionStrategyPayload()` -> `codex_strategy_runner` dry-run payload -> local `workflow.advance` result
- verification-phase task contract -> pending `harness.verify` command -> verifier gating -> review-facing result
