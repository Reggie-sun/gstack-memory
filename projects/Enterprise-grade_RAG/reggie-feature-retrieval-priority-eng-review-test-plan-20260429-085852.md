# Test Plan
Generated by /plan-eng-review on 2026-04-29
Branch: feature-retrieval-priority
Repo: Enterprise-grade_RAG

## Affected Pages/Routes
- `sdk/agent-runtime-host/index.mjs` eligibility gate, verify symptom-style repo prompt admission and rejection boundaries.
- `scripts/codex_runtime_nl_entry.mjs --check-entry`, verify CLI-visible entry decisions and trace behavior.

## Key Interactions to Verify
- In repo context, a symptom prompt like `Office PDF 预览失败（HTTP 500）` enters `runtime-workflow`.
- Without repo context, the same symptom prompt is rejected with a structured blocked reason.
- `explicitSkill=runtime-workflow` bypasses passive gate rejection and enters runtime.

## Edge Cases
- Symptom prompt with only raw text and no `activeFile`, `openTabs`, `changedPaths`, or `taskPath`.
- Non-repo prompt with repo context still stays rejected.
- Existing direct-edit and explicit-lane bypass cases stay unchanged after the new symptom gate lands.

## Critical Paths
- Helper path: `evaluateRuntimeWorkflowEntry()` returns admit/reject decisions for symptom prompts.
- CLI path: `codex_runtime_nl_entry.mjs --check-entry` mirrors helper behavior for the same prompts.
